export enum FabCollection {
  common = "common",
  users = "users",
  posts = "posts",
  forums = "forums",
  threads = "threads",
  categories = "categories",
}

export enum FabCollectionGenericTypes {
  stats = "stats",
}
